function Loading() {
  return (
    <div
      style={{
        padding: '2rem',
        textAlign: 'center',
        fontSize: '1rem',
      }}
      role="status"
      aria-live="polite"
    >
      Loading...
    </div>
  );
}

export default Loading;
